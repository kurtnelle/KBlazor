@* KBlazor.Showcase/Pages/GettingStarted.razor *@
@page "/getting-started"

<PageTitle>Getting Started — KBlazor</PageTitle>

<div class="gs-layout">
    <NavMenu />
    <div class="gs-page">

    <div class="gs-hero">
        <div class="gs-hero-badge">
            <span class="gs-badge-dot"></span>
            Installation Guide
        </div>
        <h1>Get up and running</h1>
        <p>KBlazor is hosted at <strong>kblazor.com</strong>. Follow the steps below to add it to your Blazor Server project.</p>
    </div>

    <!-- ── Step 1: Add package source ────────────────────────────────── -->
    <div class="gs-step">
        <div class="gs-step-num">1</div>
        <div class="gs-step-body">
            <h2>Add the NuGet package source</h2>
            <p>KBlazor is hosted on a custom NuGet feed. Add the source to your <code>nuget.config</code> (or via the CLI):</p>

            <div class="gs-tab-group">
                <div class="gs-tab-label">nuget.config</div>
                <pre class="gs-code lang-xml"><span class="cx">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span>
<span class="cx">&lt;configuration&gt;</span>
<span class="cx">  &lt;packageSources&gt;</span>
<span class="cx">    &lt;add key="nuget.org" value="https://api.nuget.org/v3/index.json" /&gt;</span>
<span class="ck">    &lt;add key="KBlazorFeed" value="<span class="cv">https://nuget.kblazor.com/v3/index.json</span>" /&gt;</span>
<span class="cx">  &lt;/packageSources&gt;</span>
<span class="cx">&lt;/configuration&gt;</span></pre>
            </div>

            <div class="gs-tab-group" style="margin-top:12px;">
                <div class="gs-tab-label">.NET CLI</div>
                <pre class="gs-code lang-sh"><span class="ck">dotnet</span> nuget add source <span class="cv">https://nuget.kblazor.com/v3/index.json</span> --name KBlazorFeed</pre>
            </div>

            <div class="gs-callout">
                The feed URL is <code>https://nuget.kblazor.com/v3/index.json</code> — no authentication required.
            </div>
        </div>
    </div>

    <!-- ── Step 2: Install the package ───────────────────────────────── -->
    <div class="gs-step">
        <div class="gs-step-num">2</div>
        <div class="gs-step-body">
            <h2>Install the package</h2>
            <p>Add <strong>KBlazor</strong> to your project using your preferred tool.</p>

            <div class="gs-tab-group">
                <div class="gs-tab-label">.NET CLI</div>
                <pre class="gs-code lang-sh"><span class="ck">dotnet</span> add package KBlazor</pre>
            </div>

            <div class="gs-tab-group" style="margin-top:12px;">
                <div class="gs-tab-label">Package Manager Console</div>
                <pre class="gs-code lang-sh"><span class="ck">Install-Package</span> KBlazor</pre>
            </div>

            <div class="gs-tab-group" style="margin-top:12px;">
                <div class="gs-tab-label">PackageReference (csproj)</div>
                <pre class="gs-code lang-xml"><span class="cx">&lt;PackageReference Include="KBlazor" Version="<span class="cv">*</span>" /&gt;</span></pre>
            </div>

            <div class="gs-callout gs-callout--info">
                KBlazor requires <strong>MudBlazor 8.x</strong> and <strong>.NET 8</strong> or later.
                If MudBlazor isn't in your project yet, it will be added automatically as a transitive dependency.
            </div>
        </div>
    </div>

    <!-- ── Step 3: Register services ─────────────────────────────────── -->
    <div class="gs-step">
        <div class="gs-step-num">3</div>
        <div class="gs-step-body">
            <h2>Register services in <code>Program.cs</code></h2>
            <p>KBlazor needs three scoped services and MudBlazor's service collection.</p>

            <div class="gs-tab-group">
                <div class="gs-tab-label">Program.cs</div>
                <pre class="gs-code lang-cs"><span class="ck">using</span> KBlazor.Services;
<span class="ck">using</span> MudBlazor.Services;

<span class="cx">// MudBlazor (required by KBlazor components)</span>
builder.Services.<span class="cv">AddMudServices</span>();
builder.Services.<span class="cv">AddHttpContextAccessor</span>();

<span class="cx">// KBlazor — provide your own implementations or use the in-memory stubs</span>
builder.Services.AddScoped&lt;<span class="cv">IFlexTableSettings</span>,    YourFlexTableSettings&gt;();
builder.Services.AddScoped&lt;<span class="cv">IListViewSettingStore</span>, YourListViewSettingStore&gt;();
builder.Services.AddScoped&lt;<span class="cv">IEntityLookupProvider</span>, YourEntityLookupProvider&gt;();</pre>
            </div>

            <div class="gs-service-cards">
                <div class="gs-service-card">
                    <div class="gs-service-name">IFlexTableSettings</div>
                    <div class="gs-service-desc">Controls whether personal views are enabled and which roles have admin access.</div>
                </div>
                <div class="gs-service-card">
                    <div class="gs-service-name">IListViewSettingStore</div>
                    <div class="gs-service-desc">Persists named FlexTable views (column selection, sort, filters) per user.</div>
                </div>
                <div class="gs-service-card">
                    <div class="gs-service-name">IEntityLookupProvider</div>
                    <div class="gs-service-desc">Powers the <code>[AutoComplete]</code> attribute — returns search results for related entities.</div>
                </div>
            </div>
        </div>
    </div>

    <!-- ── Step 4: Add imports ────────────────────────────────────────── -->
    <div class="gs-step">
        <div class="gs-step-num">4</div>
        <div class="gs-step-body">
            <h2>Add namespaces to <code>_Imports.razor</code></h2>
            <p>Add these usings so every component in your project can reference KBlazor types without qualification.</p>

            <div class="gs-tab-group">
                <div class="gs-tab-label">_Imports.razor</div>
                <pre class="gs-code lang-cs"><span class="ck">@@using</span> KBlazor.Components
<span class="ck">@@using</span> KBlazor.Models
<span class="ck">@@using</span> KBlazor.Services
<span class="ck">@@using</span> MudBlazor</pre>
            </div>
        </div>
    </div>

    <!-- ── Step 5: Decorate your model ─────────────────────────────────── -->
    <div class="gs-step">
        <div class="gs-step-num">5</div>
        <div class="gs-step-body">
            <h2>Decorate your model with attributes</h2>
            <p>KBlazor reads <code>[Display]</code> attributes to build columns and form fields automatically. Additional attributes control editing behaviour, sorting, and rendering.</p>

            <div class="gs-tab-group">
                <div class="gs-tab-label">Order.cs</div>
                <pre class="gs-code lang-cs"><span class="ck">public class</span> <span class="cv">Order</span>
{
    [<span class="cv">Display</span>(Name = <span class="cx">"Order #"</span>, Order = 1)]
    [<span class="cv">ReadOnlyOnEdit</span>]
    <span class="ck">public string</span> Name { <span class="ck">get</span>; <span class="ck">set</span>; }

    [<span class="cv">AutoComplete</span>]
    <span class="ck">public</span> Guid? CustomerId { <span class="ck">get</span>; <span class="ck">set</span>; }
    <span class="ck">public virtual</span> Customer? Customer { <span class="ck">get</span>; <span class="ck">set</span>; }

    <span class="cx">// Computed property — displays customer name, sorts on Customer.Name</span>
    [<span class="cv">Display</span>(Name = <span class="cx">"Customer"</span>, Order = 2)]
    [<span class="cv">SortAndFilterOn</span>(Member = <span class="cx">"Customer.Name"</span>)]
    <span class="ck">public string</span> CustomerName =&gt; Customer?.Name ?? <span class="ck">string</span>.Empty;

    [<span class="cv">Display</span>(Name = <span class="cx">"Amount"</span>, Order = 3)]
    [<span class="cv">DisplayNoWrap</span>]
    <span class="ck">public decimal</span> Amount { <span class="ck">get</span>; <span class="ck">set</span>; }

    [<span class="cv">Display</span>(Name = <span class="cx">"Notes"</span>, Order = 4)]
    [<span class="cv">MemoDisplay</span>]
    <span class="ck">public string</span> Notes { <span class="ck">get</span>; <span class="ck">set</span>; }
}</pre>
            </div>

            <div class="gs-callout">
                The <code>Fields</code> parameter on FlexTable and BasicEdit matches against
                <code>[Display(Name = "...")]</code> values. See the
                <a href="/demo/attributes" style="color:var(--accent);">Attributes reference</a>
                for the full list of available attributes.
            </div>
        </div>
    </div>

    <!-- ── Step 6: Done ───────────────────────────────────────────────── -->
    <div class="gs-step gs-step--last">
        <div class="gs-step-num gs-step-num--done">✓</div>
        <div class="gs-step-body">
            <h2>You're ready</h2>
            <p>Drop a component into any page and start building.</p>

            <div class="gs-tab-group">
                <div class="gs-tab-label">Example.razor</div>
                <pre class="gs-code lang-cs"><span class="cx">&lt;FlexTable TItem="Order"</span>
<span class="cx">           Items="@@orders"</span>
<span class="cx">           Fields="Order #,Customer,Amount,Notes"</span>
<span class="cx">           SortFilter="OnSortFilter" /&gt;</span></pre>
            </div>

            <div class="gs-cta-row">
                <a href="/demo/flextable" class="gs-btn-primary">FlexTable demo</a>
                <a href="/demo/basicedit" class="gs-btn-secondary">BasicEdit demo</a>
                <a href="/demo/attributes" class="gs-btn-secondary">Attributes reference</a>
            </div>
        </div>
    </div>

    </div>
</div>

<style>
    .gs-layout { display: flex; min-height: calc(100vh - 64px); }
    .gs-page {
        flex: 1;
        max-width: 860px;
        margin: 0 auto;
        padding: 60px 40px 100px;
        display: flex;
        flex-direction: column;
        gap: 0;
    }

    /* ── Hero ── */
    .gs-hero {
        display: flex;
        flex-direction: column;
        gap: 16px;
        padding-bottom: 56px;
    }
    .gs-hero-badge {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: rgba(108,92,231,0.1);
        border: 1px solid rgba(108,92,231,0.2);
        border-radius: 999px;
        padding: 6px 16px;
        font-size: 12px;
        color: var(--text-muted);
        width: fit-content;
        font-weight: 500;
    }
    .gs-badge-dot {
        width: 8px; height: 8px;
        background: var(--accent);
        border-radius: 50%;
        display: inline-block;
        box-shadow: 0 0 8px var(--accent-glow);
    }
    .gs-hero h1 {
        font-size: 42px;
        font-weight: 800;
        line-height: 1.1;
        margin: 0;
        color: #fff;
    }
    .gs-hero p {
        font-size: 15px;
        color: var(--text-muted);
        line-height: 1.7;
        margin: 0;
        max-width: 560px;
    }

    /* ── Steps ── */
    .gs-step {
        display: flex;
        gap: 28px;
        padding: 0 0 48px;
        position: relative;
    }
    .gs-step:not(.gs-step--last)::before {
        content: '';
        position: absolute;
        left: 18px;
        top: 44px;
        bottom: 0;
        width: 2px;
        background: linear-gradient(to bottom, var(--accent), transparent);
        opacity: 0.25;
    }
    .gs-step-num {
        width: 38px; height: 38px;
        min-width: 38px;
        border-radius: 50%;
        background: rgba(108,92,231,0.12);
        border: 2px solid var(--accent);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 15px;
        font-weight: 700;
        color: var(--accent);
        margin-top: 2px;
    }
    .gs-step-num--done {
        background: #1A3A28;
        border-color: #4ADE80;
        color: #4ADE80;
        font-size: 16px;
    }
    .gs-step-body {
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: 14px;
    }
    .gs-step-body h2 {
        font-size: 20px;
        font-weight: 700;
        color: #fff;
        margin: 0;
        padding-top: 6px;
    }
    .gs-step-body p {
        font-size: 14px;
        color: var(--text-muted);
        line-height: 1.7;
        margin: 0;
    }

    /* ── Code blocks ── */
    .gs-tab-group {
        border-radius: 12px;
        overflow: hidden;
        border: 1px solid var(--border);
    }
    .gs-tab-label {
        background: #13112A;
        padding: 6px 14px;
        font-size: 11px;
        font-weight: 600;
        letter-spacing: 0.5px;
        color: var(--text-muted);
        border-bottom: 1px solid var(--border);
        text-transform: uppercase;
    }
    .gs-code {
        background: #0D0C1A;
        margin: 0;
        padding: 16px 18px;
        font-family: var(--mono);
        font-size: 13px;
        line-height: 1.75;
        overflow-x: auto;
        color: #C8C2F0;
        white-space: pre;
    }
    .gs-code .ck  { color: #B4A8FF; }   /* keyword / command */
    .gs-code .cv  { color: #7EDBB4; }   /* value / highlight */
    .gs-code .cx  { color: #9B95C9; }   /* neutral text */

    /* ── Callouts ── */
    .gs-callout {
        background: #1A1830;
        border-left: 3px solid var(--accent);
        border-radius: 0 8px 8px 0;
        padding: 10px 14px;
        font-size: 13px;
        color: var(--text-muted);
        line-height: 1.6;
    }
    .gs-callout--info {
        border-left-color: #79D4A5;
    }
    .gs-callout code,
    .gs-step-body p code,
    .gs-step-body h2 code {
        background: rgba(108,92,231,0.1);
        border-radius: 4px;
        padding: 1px 6px;
        font-size: 12px;
        color: #B4A8FF;
        font-family: var(--mono);
    }

    /* ── Service cards ── */
    .gs-service-cards {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }
    .gs-service-card {
        background: #13112A;
        border: 1px solid var(--border);
        border-radius: 8px;
        padding: 12px 16px;
        display: flex;
        align-items: baseline;
        gap: 16px;
    }
    .gs-service-name {
        font-family: var(--mono);
        font-size: 13px;
        color: #B4A8FF;
        min-width: 220px;
        font-weight: 600;
    }
    .gs-service-desc {
        font-size: 13px;
        color: var(--text-muted);
        line-height: 1.5;
    }
    .gs-service-desc code {
        background: #1E1A3A;
        border-radius: 4px;
        padding: 1px 5px;
        font-size: 12px;
        color: #B4A8FF;
        font-family: var(--mono);
    }

    /* ── CTA row ── */
    .gs-cta-row {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        padding-top: 4px;
    }
    .gs-btn-primary {
        display: flex; align-items: center;
        height: 42px; padding: 0 22px;
        background: linear-gradient(135deg, #6C5CE7 0%, #A855F7 100%);
        color: #fff !important;
        border-radius: 10px;
        font-size: 14px; font-weight: 600;
        text-decoration: none !important;
        box-shadow: 0 2px 12px rgba(108,92,231,0.25);
        transition: all 0.2s ease;
    }
    .gs-btn-secondary {
        display: flex; align-items: center;
        height: 42px; padding: 0 22px;
        background: rgba(108,92,231,0.08);
        border: 1px solid rgba(108,92,231,0.2);
        color: var(--text-muted) !important;
        border-radius: 10px;
        font-size: 14px; font-weight: 600;
        text-decoration: none !important;
        transition: all 0.2s ease;
    }
    .gs-btn-primary:hover  { box-shadow: 0 4px 20px rgba(108,92,231,0.4); transform: translateY(-1px); }
    .gs-btn-secondary:hover { color: #fff !important; border-color: rgba(108,92,231,0.4); background: rgba(108,92,231,0.15); }
</style>
